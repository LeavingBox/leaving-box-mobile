# Avancement du 17/12/2025

- Gestion des solutions côté opérateur : récupération de `solutionsByAnalyst` dans le listener `gameStarted`, filtré via `Socket.id`, puis fusion avec les `moduleManuals` pour ne passer que les solutions de l’opérateur courant à l’écran manuel.
- Enrichissement du type `ModuleManual` avec `_id`/`moduleId` et `solutions` pour aligner le front sur le payload back.
- Normalisation des règles et solutions (string ou tableau) dans `ModuleInstructions`, affichage des blocs Règles et Solutions sans crash.
- Navigation opérateur : passage des modules enrichis à `/analyst/manual` après appariement solutions/modules.
